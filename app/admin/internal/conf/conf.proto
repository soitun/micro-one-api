syntax = "proto3";

package api.admin.v1;

option go_package = "micro-one-api/app/admin/internal/conf;conf";

// Bootstrap is the top-level configuration loaded from config.yaml.
message Bootstrap {
  Server server = 1;
  Clients clients = 2;
  Data data = 3;
  Registry registry = 4;
}

// Server holds the HTTP and gRPC server configuration.
message Server {
  HTTP http = 1;
  GRPC grpc = 2;
}

message HTTP {
  string addr = 1 [json_name = "addr"];
  string web_root = 2 [json_name = "web_root"];
}

message GRPC {
  string addr = 1 [json_name = "addr"];
}

// Data holds the database configuration for system options storage.
message Data {
  Database database = 1;
}

message Database {
  string driver = 1 [json_name = "driver"];
  string source = 2 [json_name = "source"];
  // Schema isolates this service to a specific database schema (Phase 2.4).
  string schema = 3 [json_name = "schema"];
}

// Clients holds the client endpoint configuration.
message Clients {
  Identity identity = 1 [json_name = "identity"];
  Channel channel = 2 [json_name = "channel"];
  Billing billing = 3 [json_name = "billing"];
}

message Identity {
  string endpoint = 1 [json_name = "endpoint"];
  string http_endpoint = 2 [json_name = "http_endpoint"];
}

message Channel {
  string endpoint = 1 [json_name = "endpoint"];
}

message Billing {
  string endpoint = 1 [json_name = "endpoint"];
}

// Registry holds the service registry configuration.
message Registry {
  string type = 1 [json_name = "type"];
  Consul consul = 2 [json_name = "consul"];
  map<string, string> metadata = 3 [json_name = "metadata"];
}

message Consul {
  string address = 1 [json_name = "address"];
  int64 health_check_interval = 2 [json_name = "health_check_interval"];
}
